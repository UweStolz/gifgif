import {
  getAllData, getCountOfGifs, getGifData, removeCompleteGifData, removeGifData, setGifdata,
} from './gifActions';
import { getAllConfigs, getConfig, setConfig } from './configActions';

export default {
  getAllData,
  getCountOfGifs,
  getGifData,
  removeCompleteGifData,
  removeGifData,
  setGifdata,
  getAllConfigs,
  getConfig,
  setConfig,
};
